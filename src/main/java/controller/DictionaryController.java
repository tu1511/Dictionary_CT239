import javax.swing.JOptionPane;
import model.DictionaryModel;

public class DictionaryController {
    private DictionaryModel model;

//    public DictionaryController(DictionaryModel model, DictionaryDeletedWords view) {
//        this.model = model;
//        this.view = view;
//        this.view.displayDeletedWords(model.getList());
//    }

//    public void recoverWord(String selectedWord) {
//        if (model.recoverWord(selectedWord)) {
//            view.refreshTable(model.getList());
//            JOptionPane.showMessageDialog(view, "Dữ liệu đã được khôi phục!", "Thông báo", JOptionPane.INFORMATION_MESSAGE);
//        } else {
//            JOptionPane.showMessageDialog(view, "Không thể khôi phục từ \"" + selectedWord + "\"!", "Lỗi", JOptionPane.ERROR_MESSAGE);
//        }
//    }
//
//    public void deleteWord(String selectedWord) {
//        if (model.deleteWord(selectedWord)) {
//            view.refreshTable(model.getList());
//            JOptionPane.showMessageDialog(view, "Dữ liệu đã được xóa vĩnh viễn!", "Thông báo", JOptionPane.INFORMATION_MESSAGE);
//        } else {
//            JOptionPane.showMessageDialog(view, "Không thể xóa từ \"" + selectedWord + "\"!", "Lỗi", JOptionPane.ERROR_MESSAGE);
//        }
//    }
    
    // Các phương thức xử lý sự kiện khác...
}
